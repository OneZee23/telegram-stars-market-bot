# Edge Cases и обработка ошибок

## Общие принципы

1. Все ошибки логируются в структурированном виде
2. Пользователь всегда получает понятное сообщение о статусе операции
3. Критичные ошибки требуют ручного вмешательства (уведомление администратору)
4. Приветственное сообщение содержит информацию о возможных проблемах и способах их решения

---

## Сценарии оплаты

### 1. Пользователь не оплатил заказ в течение времени ожидания

**Сценарий:**
- Пользователь создал заказ (status: PENDING)
- Получил ссылку на оплату от YooKassa
- Не оплатил в течение 30 минут (или другого таймаута)

**Обработка:**
- Заказ автоматически переходит в статус `CANCELLED` через таймаут
- Пользователю отправляется уведомление: "Заказ отменён из-за истечения времени ожидания оплаты"
- Ссылка на оплату становится недействительной

**Техническая реализация:**
- Cron job или scheduled task проверяет заказы со статусом PENDING старше 30 минут
- Автоматическая отмена через YooKassa API (если возможно)

---

### 2. Пользователь оплатил, но webhook от YooKassa не пришёл

**Сценарий:**
- Пользователь успешно оплатил через YooKassa
- Webhook не доставлен или потерян
- Заказ остаётся в статусе PENDING

**Обработка:**
- Регулярная проверка статуса платежей в YooKassa (polling)
- Если платеж подтверждён в YooKassa, но заказ в статусе PENDING → обновить статус вручную
- Логирование для анализа причин потери webhook

**Техническая реализация:**
- Scheduled task каждые 5-10 минут проверяет заказы со статусом PENDING
- Запрос статуса платежа через YooKassa API
- Автоматическое обновление статуса заказа при обнаружении оплаченного платежа

---

### 3. Пользователь оплатил, но webhook пришёл дважды (дубликат)

**Сценарий:**
- YooKassa отправила webhook дважды
- Оба webhook обрабатываются

**Обработка:**
- Проверка idempotency: проверять, не обработан ли уже этот платеж
- Игнорировать дубликаты
- Логировать дубликаты для мониторинга

**Техническая реализация:**
- Хранение обработанных payment_id в базе данных
- Проверка перед обработкой webhook: `SELECT * FROM orders WHERE payment_id = ?`
- Если заказ уже в статусе PAID или выше → игнорировать webhook

---

### 4. Пользователь отменил оплату в YooKassa

**Сценарий:**
- Пользователь начал оплату, но отменил её
- YooKassa отправляет webhook с статусом "cancelled"

**Обработка:**
- Заказ переходит в статус `CANCELLED`
- Пользователю отправляется уведомление: "Оплата отменена. Заказ не выполнен."
- Возможность создать новый заказ

**Техническая реализация:**
- Обработка webhook с event_type = "payment.canceled"
- Обновление статуса заказа
- Уведомление пользователя через Telegram Bot API

---

### 5. Ошибка при создании платежа в YooKassa

**Сценарий:**
- Backend пытается создать платеж в YooKassa
- YooKassa API возвращает ошибку (недостаточно средств на счёте магазина, неверные данные и т.д.)

**Обработка:**
- Заказ не создаётся или создаётся со статусом `FAILED`
- Пользователю отправляется сообщение: "Произошла ошибка при создании платежа. Попробуйте позже или обратитесь в поддержку."
- Логирование ошибки с деталями
- Уведомление администратору о критичной ошибке

**Техническая реализация:**
- Try-catch при вызове YooKassa API
- Retry логика для временных ошибок (network issues)
- Алерт администратору при критичных ошибках

---

## Сценарии покупки Stars через Fragment

### 6. Fragment API вернул ошибку при покупке Stars

**Сценарий:**
- Заказ оплачен (status: PAID)
- Backend пытается купить Stars через Fragment API
- Fragment возвращает ошибку (недостаточно TON, проблемы с KYC, временная недоступность API)

**Обработка:**
- Заказ переходит в статус `FAILED`
- Логирование ошибки с деталями
- Уведомление администратору о необходимости ручного вмешательства
- Пользователю отправляется сообщение: "Произошла ошибка при обработке заказа. Мы уже работаем над решением. Ваши средства будут возвращены в течение 24 часов."

**Техническая реализация:**
- Try-catch при вызове Fragment API
- Retry логика для временных ошибок (с экспоненциальной задержкой)
- Автоматический возврат средств через YooKassa API (refund)
- Алерт администратору

---

### 7. Stars куплены через Fragment, но не отправлены пользователю

**Сценарий:**
- Fragment API успешно вернул transaction ID
- Backend пытается отправить Stars через Telegram API
- Telegram API возвращает ошибку (пользователь заблокировал бота, неверный user_id и т.д.)

**Обработка:**
- Заказ остаётся в статусе `PROCESSING`
- Логирование ошибки
- Уведомление администратору
- Попытка повторной отправки через 1 час, 6 часов, 24 часа
- Если после 3 попыток не удалось → ручное вмешательство

**Техническая реализация:**
- Retry механизм для отправки Stars
- Хранение количества попыток в базе данных
- Scheduled task для повторных попыток
- Алерт администратору при превышении лимита попыток

---

### 8. Stars отправлены пользователю, но он не получил их

**Сценарий:**
- Telegram API вернул успешный ответ
- Пользователь утверждает, что не получил Stars

**Обработка:**
- Проверка логов: был ли успешный ответ от Telegram API
- Проверка transaction ID в Fragment: действительно ли Stars были куплены
- Если подтверждено, что Stars были отправлены → объяснить пользователю возможные причины (задержка Telegram, проверка в настройках)
- Если ошибка подтверждена → ручное возмещение Stars администратором

**Техническая реализация:**
- Сохранение всех transaction ID (fragmentTxId, telegramTxId) в базе данных
- Логирование всех успешных операций
- Админ-панель для проверки статуса заказов
- Возможность ручной отправки Stars администратором

---

## Сценарии выбора получателя Stars

### 9. Пользователь выбрал "Себе", но у него нет username

**Сценарий:**
- Пользователь выбрал покупку Stars себе
- Backend проверяет username через `getChat` Bot API
- Username отсутствует (null или пустая строка)

**Обработка:**
- Пользователю отправляется сообщение с инструкцией по установке username
- Заказ не создаётся
- Пользователь может повторить попытку после установки username

**Сообщение пользователю:**
```
Для покупки Stars себе необходимо установить username в настройках Telegram.

Как установить username:
1. Откройте настройки Telegram
2. Перейдите в "Имя пользователя" (Username)
3. Установите уникальный username
4. Вернитесь в бота и попробуйте снова

После установки username используйте команду /start
```

**Техническая реализация:**
- Проверка через `getChat(chat_id)` → проверка поля `username`
- Если username отсутствует → отправка инструкции
- Сохранение состояния в сессии пользователя (опционально)

---

### 10. Пользователь выбрал "Другому", но ввёл несуществующий username

**Сценарий:**
- Пользователь выбрал покупку Stars другому пользователю
- Ввёл username (например, "nonexistent_user")
- Backend проверяет через Fragment API
- Username не существует (404 или аналогичная ошибка)

**Обработка:**
- Пользователю отправляется сообщение об ошибке
- Запрос на повторный ввод username
- Валидация формата username (только латиница, цифры, подчёркивания, длина)

**Сообщение пользователю:**
```
❌ Username "@{username}" не найден.

Проверьте правильность написания username:
- Username должен существовать в Telegram
- Вводите без символа @
- Используйте только латиницу, цифры и подчёркивания

Попробуйте ввести username ещё раз:
```

**Техническая реализация:**
- Валидация формата username перед запросом к Fragment API
- Запрос к Fragment API для проверки существования
- Если 404 или username не найден → ошибка и повторный запрос
- Ограничение на количество попыток (например, 3 попытки)

---

### 11. Пользователь ввёл username с символом @

**Сценарий:**
- Пользователь ввёл username с символом @ (например, "@username")
- Backend должен обработать это корректно

**Обработка:**
- Автоматическое удаление символа @ из начала строки
- Продолжение проверки username
- Логирование для статистики (сколько пользователей вводят с @)

**Техническая реализация:**
- Trim и удаление @ из начала строки перед валидацией
- Нормализация username перед сохранением в базу

---

### 12. Fragment API недоступен при проверке username

**Сценарий:**
- Пользователь ввёл username для проверки
- Fragment API недоступен (timeout, 500 error, network error)

**Обработка:**
- Retry механизм (3 попытки с экспоненциальной задержкой)
- Если все попытки неудачны → сообщение пользователю о временной недоступности
- Предложение повторить позже

**Сообщение пользователю:**
```
⚠️ Сервис проверки username временно недоступен.

Пожалуйста, попробуйте через несколько минут.

Если проблема сохраняется, обратитесь в поддержку: @your_support_username
```

**Техническая реализация:**
- Retry логика с экспоненциальной задержкой (1s, 2s, 4s)
- Логирование всех ошибок Fragment API
- Алерт администратору при множественных ошибках

---

### 13. Пользователь изменил username после создания заказа

**Сценарий:**
- Пользователь создал заказ на покупку Stars себе
- Username был сохранён в заказе (например, "old_username")
- Пользователь изменил username в Telegram (теперь "new_username")
- Заказ обрабатывается и пытается купить Stars на старый username

**Обработка:**
- При покупке Stars через Fragment API использовать username из заказа (recipientUsername)
- Если покупка на себя (isSelfPurchase: true) → проверка актуального username перед отправкой через Telegram API
- Если username изменился → обновить username в заказе или использовать telegramId для отправки

**Техническая реализация:**
- При обработке заказа с isSelfPurchase: true → проверка актуального username через getChat
- Если username изменился → обновление recipientUsername в заказе
- Использование telegramId как fallback для отправки Stars через Telegram API

---

### 14. Пользователь ввёл username с опечаткой, но он существует (другой пользователь)

**Сценарий:**
- Пользователь хотел отправить Stars пользователю "john_doe"
- Ввёл "john_do" (опечатка)
- Username "john_do" существует в Telegram
- Fragment API подтверждает существование
- Stars будут отправлены не тому пользователю

**Обработка:**
- Показать пользователю подтверждение перед созданием заказа:
  "Вы хотите отправить {amount} Stars пользователю @{username}?"
- Кнопки "Подтвердить" / "Отменить"
- Только после подтверждения создавать заказ

**Техническая реализация:**
- После проверки username → показ подтверждения с username
- Сохранение выбранного username в сессии/состоянии
- Создание заказа только после подтверждения

---

## Сценарии работы бота

### 15. Пользователь заблокировал бота во время обработки заказа

**Сценарий:**
- Заказ в процессе обработки (status: PROCESSING)
- Пользователь заблокировал бота
- Невозможно отправить уведомление о завершении

**Обработка:**
- Логирование факта блокировки
- Заказ всё равно обрабатывается до конца
- При разблокировке бота пользователь может запросить статус через `/status`
- Уведомление администратору (опционально)

**Техническая реализация:**
- Обработка ошибки "chat not found" или "user is deactivated" от Telegram API
- Продолжение обработки заказа независимо от статуса бота
- Сохранение всех данных заказа в базе

---

### 16. Пользователь создал несколько заказов одновременно

**Сценарий:**
- Пользователь быстро нажал кнопку "Подтвердить" несколько раз
- Создано несколько заказов на одну и ту же сумму

**Обработка:**
- Защита от дублирования: проверка активных заказов пользователя
- Если есть заказ в статусе PENDING → предложить оплатить существующий или отменить его
- Ограничение: максимум 1 активный заказ (PENDING или PROCESSING) на пользователя

**Техническая реализация:**
- Проверка перед созданием заказа: `SELECT * FROM orders WHERE user_id = ? AND status IN ('PENDING', 'PROCESSING')`
- Если активный заказ найден → сообщение пользователю с предложением оплатить существующий
- Опционально: автоматическая отмена старых заказов в статусе PENDING

---

### 17. Неверные данные от пользователя

**Сценарий:**
- Пользователь выбрал неверное количество Stars (например, через прямой вызов API)
- Backend получил некорректные данные

**Обработка:**
- Валидация всех входящих данных через DTO (class-validator)
- Отклонение неверных запросов с понятным сообщением об ошибке
- Логирование попыток отправки неверных данных

**Техническая реализация:**
- DTO с валидацией (минимальное/максимальное количество Stars)
- Глобальный exception filter в NestJS для обработки ошибок валидации
- Возврат понятных сообщений об ошибках пользователю

---

## Информирование пользователя

### Приветственное сообщение (/start)

Сообщение должно содержать:

```
Добро пожаловать в Telegram Stars Shop!

Что нужно знать:

1. Выбор получателя: вы можете купить Stars себе или отправить другому пользователю.
   - Для покупки себе необходим username в Telegram
   - Для отправки другому пользователю введите его username (без @)

2. Оплата: после подтверждения заказа вы получите ссылку на оплату. 
   Время ожидания оплаты: 30 минут.

3. Получение Stars: после успешной оплаты Stars будут отправлены 
   автоматически в течение 5-10 минут.

4. Если Stars не пришли:
   - Проверьте настройки приватности Telegram
   - Подождите до 24 часов (возможны задержки)
   - Используйте команду /status для проверки статуса заказа
   - Обратитесь в поддержку, если проблема не решена

5. Возврат средств: при отмене заказа или ошибке обработки 
   средства возвращаются автоматически в течение 24 часов.

6. Поддержка: @your_support_username

Готовы начать? Нажмите кнопку ниже для выбора количества Stars.
```

---

## Мониторинг и алерты

### Критичные ситуации, требующие уведомления администратора

1. **Ошибка при создании платежа в YooKassa** (более 3 подряд)
2. **Ошибка при покупке Stars через Fragment** (любая)
3. **Не удалось отправить Stars пользователю** (после 3 попыток)
4. **Потеря webhook от YooKassa** (обнаружена оплата без webhook)
5. **Недостаточно TON на кошельке** для покупки Stars
6. **Ошибка возврата средств** (refund failed)
7. **Fragment API недоступен** (более 5 попыток подряд)
8. **Множественные ошибки проверки username** (более 10 подряд)

### Каналы уведомлений

**Основной канал:** Telegram группа для публичной разработки
- Бот добавлен в группу как администратор
- Все уведомления отправляются в эту группу
- Позволяет следить за проектом в реальном времени
- Успешные операции и ошибки видны всем участникам группы

**Дополнительные каналы:**
- Grafana алерты (dashboard.onezee.ru)
- Email (опционально, для критичных ошибок)

**Типы уведомлений в группу:**

1. **Успешные операции (публичные):**
   - 🎉 Поздравления с покупками
   - ✅ Успешные оплаты
   - ✅ Успешная отправка Stars

2. **Критичные ошибки (публичные):**
   - 🚨 Недостаточно TON на кошельке
   - 🚨 Ошибки YooKassa
   - 🚨 Ошибки Fragment API
   - 🚨 Ошибки возврата средств

3. **Предупреждения (публичные):**
   - ⚠️ Потеря webhook
   - ⚠️ Множественные ошибки
   - ⚠️ Проблемы с отправкой Stars

4. **Информационные (публичные):**
   - 📊 Статистика за период
   - 🎯 Достижение метрик

---

## Логирование

### Уровни логов

- **ERROR:** Критичные ошибки, требующие вмешательства
- **WARN:** Предупреждения (дубликаты webhook, повторные попытки)
- **INFO:** Успешные операции (создание заказа, оплата, отправка Stars)
- **DEBUG:** Детальная информация для отладки

### Что логировать

- Все операции с заказами (создание, изменение статуса)
- Все вызовы внешних API (YooKassa, Fragment, Telegram)
- Все ошибки с контекстом (user_id, order_id, error message)
- Все webhook'и от YooKassa (входящие и обработанные)
- Все проверки username (успешные и неудачные)
- Изменения username пользователей

### Структура логов

```json
{
  "timestamp": "2025-12-19T10:00:00Z",
  "level": "ERROR",
  "service": "payment",
  "orderId": "uuid",
  "userId": "telegram_id",
  "error": "Fragment API error",
  "details": {
    "fragmentError": "...",
    "retryCount": 2
  }
}
```

---

## Ручное вмешательство

### Админ-панель (будущая функциональность)

Возможности для администратора:

1. **Просмотр заказов:**
   - Фильтрация по статусу, пользователю, дате
   - Детальная информация о каждом заказе

2. **Ручные операции:**
   - Отмена заказа
   - Возврат средств (refund)
   - Ручная отправка Stars пользователю
   - Изменение статуса заказа

3. **Мониторинг:**
   - Статистика по заказам
   - Список проблемных заказов
   - Логи по конкретному заказу

---

## Резюме обработки ошибок

| Сценарий | Действие бекенда | Действие бота | Уведомление пользователя | Уведомление в группу |
|----------|------------------|--------------|--------------------------|---------------------|
| Не оплачен в срок | Отмена заказа | Сообщение об отмене | Да | Нет |
| Webhook не пришёл | Polling статуса | - | Нет (автоматическое исправление) | ⚠️ Предупреждение |
| Дубликат webhook | Игнорирование | - | Нет | Нет |
| Отмена оплаты | Обновление статуса | Сообщение об отмене | Да | Нет |
| Ошибка YooKassa | Логирование, алерт | Сообщение об ошибке | Да | 🚨 Критичная ошибка |
| Ошибка Fragment | Возврат средств | Сообщение + refund | Да | 🚨 Критичная ошибка |
| Ошибка отправки Stars | Retry механизм | - | После успешной отправки | ⚠️ После 3 попыток |
| Stars не дошли | Проверка логов | Инструкция пользователю | Да | Нет (только если критично) |
| Бот заблокирован | Продолжение обработки | - | При разблокировке | Нет |
| Дубликат заказа | Проверка активных заказов | Предложение оплатить существующий | Да | Нет |
| Нет username (себе) | Проверка через getChat | Инструкция по установке username | Да | Нет |
| Несуществующий username (другому) | Проверка через Fragment API | Ошибка, запрос повторного ввода | Да | Нет |
| Username с символом @ | Нормализация (удаление @) | - | Нет | Нет |
| Fragment API недоступен | Retry механизм | Сообщение о временной недоступности | Да | 🚨 После 5 попыток |
| Username изменился после заказа | Обновление recipientUsername | - | Нет (автоматическое исправление) | Нет |
| Опечатка в username (существует) | Подтверждение перед заказом | Показ подтверждения с username | Да | Нет |
| Успешная покупка | Обновление статуса | Уведомление о завершении | Да | 🎉 Поздравление |
| Недостаточно TON | Проверка баланса | - | Нет | 🚨 Критичная ошибка |

---

## Дополнительные рекомендации

1. **Тестирование edge cases:**
   - Все описанные сценарии должны быть покрыты E2E тестами
   - Моки для внешних API для симуляции ошибок

2. **Документация для пользователей:**
   - FAQ в боте (команда /help)
   - Инструкции по решению типичных проблем

3. **Метрики:**
   - Процент успешных заказов
   - Время обработки заказа
   - Количество ошибок по типам
   - Процент возвратов средств

4. **Публичная разработка:**
   - Все уведомления отправляются в Telegram группу
   - Участники группы видят успешные операции и ошибки в реальном времени
   - Это создаёт прозрачность процесса разработки
   - Помогает быстро реагировать на проблемы
   - Мотивирует участников следить за проектом

