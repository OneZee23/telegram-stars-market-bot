# 🌐 Настройка прокси для обхода Cloudflare

## 🤔 Зачем нужен прокси?

Cloudflare блокирует запросы с IP-адресов серверов (например, Digital Ocean), потому что они выглядят как боты. Прокси позволяет отправлять запросы через другой IP-адрес, который выглядит как обычный пользователь.

**Проблема:** Локально всё работает, а на VPS Digital Ocean Cloudflare возвращает 403 Forbidden.

**Решение:** Использовать прокси-сервер для запросов к Fragment API.

---

## 📍 Где взять прокси?

### Вариант 1: Бесплатные прокси (для тестирования)

**⚠️ Внимание:** Бесплатные прокси часто нестабильны и медленные. Используйте только для тестирования.

**Сайты с бесплатными прокси:**

- https://www.proxy-list.download/
- https://free-proxy-list.net/
- https://www.proxyscrape.com/

**Как использовать:**

1. Найдите HTTP прокси (не SOCKS)
2. Формат: `http://IP:PORT` (например: `http://123.45.67.89:8080`)
3. Если нужна авторизация: `http://user:pass@IP:PORT`
4. **Если Proxy6 дал формат `IP:PORT:USER:PASS`** - преобразуйте в `http://USER:PASS@IP:PORT`

### Вариант 2: Платные прокси (рекомендуется для production)

**Популярные провайдеры:**

1. **Bright Data (бывший Luminati)** - https://brightdata.com/
   - Цена: от $500/месяц
   - Качество: отличное
   - Подходит для: production

2. **Smartproxy** - https://smartproxy.com/
   - Цена: от $75/месяц
   - Качество: хорошее
   - Подходит для: production

3. **Oxylabs** - https://oxylabs.io/
   - Цена: от $300/месяц
   - Качество: отличное
   - Подходит для: production

4. **Proxy-Cheap** - https://proxy-cheap.com/
   - Цена: от $10/месяц
   - Качество: среднее
   - Подходит для: тестирование, небольшие проекты

5. **Proxy6** - https://proxy6.net/
   - Цена: от $5/месяц
   - Качество: среднее
   - Подходит для: тестирование

**Рекомендация для начала:** Попробуйте Proxy6 или Proxy-Cheap для тестирования, затем переходите на более дорогие варианты для production.

---

## 🔧 Как настроить прокси в проекте

### Шаг 1: Установить зависимость

```bash
yarn add undici
```

### Шаг 2: Добавить переменную окружения

**Для локальной разработки (.env):**

```env
# Один прокси
FRAGMENT_PROXIES=http://user:pass@proxy.example.com:8080

# Или несколько прокси через запятую
FRAGMENT_PROXIES=http://proxy1.com:8080,http://proxy2.com:8080,http://proxy3.com:8080

# Или через новую строку (многострочное значение)
FRAGMENT_PROXIES=http://proxy1.com:8080
http://proxy2.com:8080
http://proxy3.com:8080

# С авторизацией
FRAGMENT_PROXIES=http://user1:pass1@proxy1.com:8080,http://user2:pass2@proxy2.com:8080

# С указанием даты истечения и URL покупки (опционально, для оповещений)
FRAGMENT_PROXIES=http://proxy1.com:8080,http://proxy2.com:8080
FRAGMENT_PROXIES_EXPIRES_AT=13.04.26, 08:01
FRAGMENT_PROXY_PURCHASE_URL=https://your-proxy-provider.com/buy
```

**Стратегия работы:**

- Используется первый рабочий прокси из списка
- Если прокси падает, автоматически переключается на следующий
- Если все прокси не работают, выводится ошибка в лог

**Система оповещений об истечении:**

Если указана переменная `FRAGMENT_PROXIES_EXPIRES_AT`, система будет автоматически проверять срок действия прокси и выводить предупреждения в логи:

- **Формат даты**: `DD.MM.YY, HH:mm` (например, `13.04.26, 08:01` означает 13 апреля 2026, 08:01)
- **30+ дней до истечения**: информационное сообщение (log level: info)
- **14-30 дней**: предупреждение (log level: warn)
- **7-14 дней**: критическое предупреждение (log level: warn)
- **Менее 7 дней**: ошибка с требованием обновить (log level: error)
- **Истекли**: ошибка с требованием немедленно обновить (log level: error)

Если указана переменная `FRAGMENT_PROXY_PURCHASE_URL`, все предупреждения будут содержать ссылку на сайт покупки прокси.

Проверка выполняется при старте приложения и затем каждые 24 часа.

#### Для GitHub Secrets (production)

1. Перейдите в репозиторий → **Settings** → **Secrets and variables** → **Actions**
2. Нажмите **"New repository secret"**
3. **Name:** `FRAGMENT_PROXIES`
4. **Value:** Прокси (один или несколько через запятую/новую строку)
5. Нажмите **"Add secret"**

**⚠️ Важно:**

- Если прокси не нужен, просто не добавляйте переменную. Код будет работать без прокси.

---

## 🧪 Как проверить, работает ли прокси?

1. Добавьте прокси в `.env` или GitHub Secrets
2. Задеплойте проект
3. Попробуйте сделать тестовую покупку
4. Проверьте логи:

```bash
pm2 logs telegram-stars-market-bot
```

**Если прокси работает, вы увидите:**

```
[ProxyManagerService] Proxy manager initialized with 3 proxy(ies)
[FragmentApiClientService] Using proxy: http://proxy.example.com:****
```

**Если прокси не работает, вы увидите:**

```
[FragmentApiClientService] Network error with proxy http://proxy1.com:****: ETIMEDOUT. Trying next proxy...
[ProxyManagerService] Proxy marked as unhealthy after 3 failures: http://proxy1.com:****
[FragmentApiClientService] Using proxy: http://proxy2.com:****
```

**Если все прокси не работают:**

```
[FragmentApiClientService] All proxies are unavailable. Tried: http://proxy1.com:****, http://proxy2.com:****, http://proxy3.com:****
```

**При использовании нескольких прокси:**

- Система автоматически переключится на следующий прокси при ошибках
- Неработающие прокси будут временно исключены из использования
- После успешного запроса прокси автоматически восстанавливается

---

## 💡 Альтернативные решения (если прокси не подходит)

### Вариант 1: Использовать другой VPS провайдер

Некоторые провайдеры имеют IP-адреса, которые меньше блокируются Cloudflare:

- **Hetzner** - часто меньше блокируется
- **OVH** - хорошая репутация IP
- **AWS/GCP** - иногда лучше проходят

### Вариант 2: Использовать резидентный прокси

Резидентные прокси используют IP-адреса домашних пользователей, что выглядит более естественно для Cloudflare.

**Провайдеры резидентных прокси:**

- Bright Data (Residential)
- Smartproxy (Residential)
- Oxylabs (Residential)

### Вариант 3: Использовать VPN

Можно настроить VPN на сервере и маршрутизировать запросы через него. Но это сложнее в настройке.

---

## ❓ FAQ

### Нужен ли прокси обязательно?

**Нет.** Прокси опционален. Если запросы проходят без прокси, он не нужен.

### Какой формат прокси нужен?

Формат: `http://[user:pass@]host:port`

Примеры:

- `http://proxy.example.com:8080`
- `http://user:password@proxy.example.com:8080`

### Поддерживаются ли SOCKS прокси?

**Нет.** Только HTTP/HTTPS прокси.

### Можно ли использовать несколько прокси?

**Да!** Поддерживается несколько прокси с автоматическим failover. Используйте переменную `FRAGMENT_PROXIES` и укажите прокси через запятую или новую строку.

**Как это работает:**

- Используется первый рабочий прокси из списка
- Если прокси падает (после 3 последовательных ошибок), автоматически переключается на следующий
- Если все прокси не работают, выводится ошибка в лог

**Преимущества нескольких прокси:**

- Автоматический failover при сбое прокси
- Отслеживание работоспособности прокси
- Автоматическое исключение неработающих прокси

### Что делать, если прокси не работает?

1. Проверьте правильность формата
2. Убедитесь, что прокси доступен с вашего сервера
3. Проверьте логи на наличие ошибок
4. Попробуйте другой прокси

---

## 📝 Примеры конфигурации

### Пример 1: Один прокси без авторизации

```env
FRAGMENT_PROXIES=http://123.45.67.89:8080
```

### Пример 2: Один прокси с авторизацией

```env
FRAGMENT_PROXIES=http://myuser:mypass@123.45.67.89:8080
```

### Пример 3: Несколько прокси

```env
FRAGMENT_PROXIES=http://proxy1.com:8080,http://proxy2.com:8080,http://proxy3.com:8080
```

### Пример 4: Несколько прокси с авторизацией

```env
FRAGMENT_PROXIES=http://user1:pass1@proxy1.com:8080,http://user2:pass2@proxy2.com:8080
```

### Пример 5: HTTPS прокси

```env
FRAGMENT_PROXIES=https://proxy.example.com:443
```

### Пример 6: Многострочный формат (удобно для большого количества прокси)

```env
FRAGMENT_PROXIES=http://proxy1.com:8080
http://proxy2.com:8080
http://proxy3.com:8080
http://proxy4.com:8080
```

### Пример 7: С указанием даты истечения и URL покупки (для оповещений)

```env
FRAGMENT_PROXIES=http://proxy1.com:8080,http://proxy2.com:8080
FRAGMENT_PROXIES_EXPIRES_AT=13.04.26, 08:01
FRAGMENT_PROXY_PURCHASE_URL=https://your-proxy-provider.com/buy
```

Формат даты: `DD.MM.YY, HH:mm` (день.месяц.год, час:минута)

---

## 🔒 Безопасность

1. **Не коммитьте прокси в git** - используйте только переменные окружения
2. **Используйте GitHub Secrets** для production
3. **Регулярно меняйте пароли** прокси, если они есть
4. **Проверяйте логи** на наличие утечек прокси

---

## 📚 Полезные ссылки

- [Документация undici](https://undici.nodejs.org/)
- [Список прокси-провайдеров](https://www.proxy-cheap.com/)
- [Cloudflare обход](https://www.scraperapi.com/blog/10-tips-for-web-scraping/)
